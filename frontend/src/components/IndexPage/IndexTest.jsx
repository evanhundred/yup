import { useDispatch } from "react-redux";

const IndexTest = () => {
  const dispatch = useDispatch();

  return (
    <div className="index-test-container">
      The simplest thing you can fetch.
    </div>
  )
};

export default IndexTest;
